const IconButton = ({icon}) => (
  <button
    type="button"
    className="pt-1 px-1 pb-2 hover:bg-lightBrown rounded-full group duration-150 ease-in focus:outline-none"
  >
    {icon}
  </button>
);

export default IconButton;
